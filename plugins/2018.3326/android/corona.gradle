android.defaultConfig {
    minSdkVersion Math.max(minSdkVersion?.apiLevel ?: 15, 24)
}
ext["minSdkVersion"] = Math.max(ext.find("minSdkVersion")?:15, 24)
dependencies {
    implementation 'com.appodeal.ads.sdk.adapters:my_target:5.51.2.0'
}
