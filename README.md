Sources for the plugin `plugin.appodeal.MyTarget`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.MyTarget" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
